export const editTask = "edit task";
import { deletTaskBtn } from "./complete-task";

//delete task

const deletButton = deletTaskBtn;

export deleteTask = deletButton.addEventListener('click', () => {
    
})